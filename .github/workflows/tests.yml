name: Run Tests

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:  # Permite ejecutar los tests manualmente desde la pestaña Actions

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: recommender
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        # Instalamos explícitamente estas para evitar el error ModuleNotFoundError visto en local
        pip install whitenoise dj-database-url 
    
    - name: Run Tests
      env:
        DB_HOST: localhost
        DB_NAME: recommender
        DB_USER: postgres
        DB_PASSWORD: postgres
        DEBUG: "False"
        CELERY_TASK_ALWAYS_EAGER: "True"  # Evita que los tests busquen Redis en la nube
      run: |
        python manage.py test products
